ENV=TEST
db_name=thesis_test
secret_key=dummy_backend_secret

file_dir="/tmp/files_unit_test"

#127.0.0.1/dummy_url
auth0_domain=test-auth0-domain
auth0_client_id=dummy_id
auth0_client_secret=dummy_secret

# in tests we'll mock calls to the API so this value really doesn't matter
openai_api_key="dummy_api_key"

invite_only_signup="false"

# disable email notifications when testing
email_from=""

# here we just double ensure the AWS API never gets used by accident (e.g. SES email)
AWS_ACCESS_KEY_ID=FAKE_ID
AWS_SECRET_ACCESS_KEY=FAKE_KEY